{
  "name": "wawa_com",
  "version": "1.0.0",
  "description": "Note: This template differs from the older `python3` template in that you don't need to worry about javascript related files such as `scrape.js` and `package.json`. This template also builds faster if you're testing it in a local docker image.",
  "main": "scrape-apify.js",
  "scripts": {
    "start": "node scrape.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "apify": "^0.21.1"
  }
}
