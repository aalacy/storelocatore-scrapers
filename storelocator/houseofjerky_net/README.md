# All locations are parsed directly from the HTML page so I ignored zip code check.

--ignore GeoConsistencyValidator

# There is no location in DE so I ignored state check.

--ignore StateLevelCountValidator